GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-120b
CHROMA_PERSIST_DIR=./chroma_db

# Demo-only: set to a persona name/id substring (e.g. "kabir") to force the
# compliance fail-then-retry loop to fire on that persona's first attempt,
# regardless of the actual computed percentages. Leave unset for organic runs.
DEMO_FORCE_FAIL_FOR=
